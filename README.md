# Orders execution bot for Crypton protocol

## Setup
- Set WALLET_PRIVATE_KEY with your wallet phrase in .env file
- Install app dependencies: `yarn` or `npm i`

## Run bot
`yarn start` or `npm run start`
